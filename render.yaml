services:
  - type: web
    name: claude-streaming-starter
    runtime: docker
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CLAUDE_MODEL
        value: claude-haiku-4-5
      - key: LOG_LEVEL
        value: INFO
    healthCheckPath: /health
